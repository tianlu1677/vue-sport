export function scrollOptions() {
  return {
    scrollOptions: {

      bounce: {
        top: false
      },
      bounceTime: 500,
      refreshDelay: 5
      // stopPropagation: true
    },
  }
}
